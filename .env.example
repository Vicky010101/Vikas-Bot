OPENAI_API_KEY=sk-...       # required if LLM_PROVIDER=openai
GEMINI_API_KEY=AIza...     # required if LLM_PROVIDER=gemini
LLM_PROVIDER=gemini        # "gemini" or "openai"
ENDEE_AUTH_TOKEN=          # leave empty if not using auth
